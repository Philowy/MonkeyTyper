#pragma once

// https://www.w3schools.com/cpp/cpp_enum.asp
enum Difficulty {
    EASY = 1,
    MEDIUM = 2,
    HARD = 3,
};

enum Stage {
    MENU = 0,
    PREGAME = 1,
    GAME = 2,
    SETTINGS = 3,
    AUTHOR = 4
};